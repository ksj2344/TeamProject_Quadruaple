<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.green.project_quadruaple.memo.MemoMapper">

    <!-- 메모 조회 -->
    <select id="selectMemo" parameterType="long" resultType="com.green.project_quadruaple.memo.model.MemoDto">
        SELECT
        memo_id AS memoId,
        trip_id AS tripId,
        day,
        seq,
        title,
        content,
        user_id AS userId,
        created_at AS createdAt,
        updated_at AS updatedAt
        FROM
        memo
        WHERE
        memo_id = #{memoId}
    </select>

    <!-- 메모 추가 -->
    <insert id="postMemo" parameterType="com.green.project_quadruaple.memo.model.MemoDto">
        INSERT INTO memo (
        trip_id, day, seq, title, content, user_id, created_at
        ) VALUES (
        #{tripId}, #{day}, #{seq}, #{title}, #{content}, #{userId}, #{createdAt}
        )
    </insert>

    <!-- 메모 작성자 조회 -->
    <select id="findMemoOwnerId" parameterType="long" resultType="long">
        SELECT user_id
        FROM memo
        WHERE memo_id = #{memoId}
    </select>

    <!-- 메모 수정 -->
    <update id="patchMemo" parameterType="com.green.project_quadruaple.memo.model.MemoDto">
        UPDATE memo
        SET
        title = #{title},
        content = #{content},
        updated_at = #{updatedAt}
        WHERE
        memo_id = #{memoId}
    </update>

    <!-- 메모 삭제 -->
    <delete id="deleteMemo" parameterType="long">
        DELETE FROM memo
        WHERE memo_id = #{memoId}
    </delete>


</mapper>